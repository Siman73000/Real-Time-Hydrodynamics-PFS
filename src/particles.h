#ifndef PARTICLES_H
#define PARTICLES_H

#include "pfs.h"
#include <stdbool.h>

// Define Sphere first so it can be used in function prototypes.
typedef struct {
    float x, y, z;
    float vx, vy, vz;
} Sphere;

// Now declare functions that use Sphere.

// Spatial Partitioning Definitions



#endif
